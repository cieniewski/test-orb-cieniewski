description: >
  This is a sample executor using Docker and Node.
docker:
  - image: 'circleci/elixir:<<parameters.version>>'
parameters:
  version:
    default: "1.10"
    description: >
      Pick a specific circleci/elixir image variant:
      https://hub.docker.com/r/cimg/elixir/tags
    type: string
